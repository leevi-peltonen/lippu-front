import { Link } from "react-router-dom";

const HomeButton = () => {
    return (
        <Link to="/">
        <button>Palaa päävalikkoon</button>
        </Link>
    );
}

export default HomeButton
